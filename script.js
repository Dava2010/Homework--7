const options = {
  strings: ["TEXT GRADIENTS", "COLORFUL TITLES", "AWESOME HEADLINES"],
  typeSpeed: 70,
  backSpeed: 40,
  startDelay: 500,
  loop: true,
};

const typed = new Typed("#typed-text", options);
